# Config do Nginx como proxy reverso com SSL (Certbot).
# 1. Copie para /etc/nginx/sites-available/pdv-restaurante
# 2. Troque SEU_DOMINIO pelo domínio real
# 3. ln -s /etc/nginx/sites-available/pdv-restaurante /etc/nginx/sites-enabled/
# 4. Primeiro boot sem SSL (comente o bloco 443, descomente o location do certbot no 80)
# 5. sudo certbot --nginx -d SEU_DOMINIO
# 6. Descomente o bloco 443, recarregue: sudo nginx -t && sudo systemctl reload nginx

# Redireciona HTTP → HTTPS
server {
    listen 80;
    server_name SEU_DOMINIO;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name SEU_DOMINIO;

    ssl_certificate /etc/letsencrypt/live/SEU_DOMINIO/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/SEU_DOMINIO/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /var/www/pdv-restaurante/frontend/dist;
    index index.html;

    # SPA: qualquer rota que não seja arquivo cai no index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket do KDS/comanda em tempo real
    location /ws {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
